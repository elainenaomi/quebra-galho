<%= form_for(ferramentum) do |f| %>
  <% if ferramentum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ferramentum.errors.count, "error") %> prohibited this ferramentum from being saved:</h2>

      <ul>
      <% ferramentum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %>
    <%= f.text_field :nome %>
  </div>

  <div class="field">
    <%= f.label :descricao %>
    <%= f.text_area :descricao %>
  </div>

  <div class="field">
    <%= f.label :disponibilidade %>
    <%= f.check_box :disponibilidade %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
