<%= form_for(emprestimo_ferramenta) do |f| %>
  <% if emprestimo_ferramenta.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(emprestimo_ferramenta.errors.count, "error") %> prohibited this emprestimo_ferramenta from being saved:</h2>

      <ul>
      <% emprestimo_ferramenta.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%#= f.label :ferramenta_id %>
    <%#= f.text_field :ferramenta_id %>
  </div>

  <h6>Ferramenta:</h6>
  <div class="field">
    <%= f.collection_select :ferramenta_id, @ferramentas, :id, :nome, {prompt: true}, {class: "browser-default"} %>
  </div>
  <p>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
